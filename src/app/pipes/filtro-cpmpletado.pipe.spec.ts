import { FiltroCpmpletadoPipe } from './filtro-cpmpletado.pipe';

describe('FiltroCpmpletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCpmpletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
